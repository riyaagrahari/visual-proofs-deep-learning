pytest>=8.0

# Tokenizer runtime: the served tokenizer is a standard HuggingFace
# `tokenizers` BPE (NFKC + Metaspace), so it loads and decodes anywhere.
tokenizers>=0.15
regex>=2024.0

# API (backend/api/) -- serving the tokenizer over HTTP.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
